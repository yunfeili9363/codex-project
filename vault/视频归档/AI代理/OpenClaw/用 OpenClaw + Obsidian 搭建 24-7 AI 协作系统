# 用 OpenClaw + Obsidian 搭建 24/7 AI 协作系统

- 来源类型：video
- 来源链接：https://www.youtube.com/watch?v=5_JN4kfr-9o
- 标签：OpenClaw, Obsidian, AI Agent, 知识管理, 工作流, 双机协作, 系统提示词, 内容创作

## 摘要
作者分享自己如何用 OpenClaw、Obsidian 和双机方案，搭建一个能 24/7 与 AI 代理协同工作的系统，并详细说明系统文件、同步方式、插件配置和任务分工思路。

## 核心要点
- 核心不是只装好 OpenClaw，而是围绕它建立可持续协作的系统。
- `user.md` 被作者视为最关键的系统文件，是个人与业务上下文的单一事实源。
- Obsidian 被用作人与代理共享的主工作区，通过 Sync 和插件实现 Markdown 之外文件的可视化与同步。
- 作者采用“个人主力机 + 代理专用 Mac mini”的双机模式，把 AI 当作逐步培养的员工来分配任务。

## 内容角度
- 如何把 AI 代理从聊天工具升级为长期协作系统
- 如何用 Obsidian 作为人类与代理共享的工作区
- 如何设计 OpenClaw 的系统文件与上下文管理
- 如何逐步把溢出任务委派给 AI 代理

## 速记卡片
# 用 OpenClaw + Obsidian 搭建 AI 协作系统

- 类型：YouTube 视频
- 链接：https://www.youtube.com/watch?v=5_JN4kfr-9o
- 核心观点：真正有价值的不是“把 OpenClaw 跑起来”，而是围绕它建立系统文件、共享工作区、双机协作和任务分工机制。

## 关键方法
- 把 `user.md` 做成自己和业务的单一事实源。
- 用 Obsidian 打开整个 workspace，而不是只在文件夹里看一堆原始 `.md` 文件。
- 用 Obsidian Sync 在“自己的机器”和“代理的机器”之间共享同一个 Vault。
- 额外安装插件，让 `TXT / JSON / HTML` 也能在 Vault 里查看和编辑。
- 先把你一直没时间做的溢出任务交给代理，再逐步扩大职责范围。

## 作者的配置
- 自己的机器：MacBook Pro，负责亲手完成自己最想做的工作。
- 代理的机器：隔离的 Mac mini，作为 OpenClaw 24/7 运行的主基地。
- 沟通方式：iMessage。
- 共享空间：Obsidian Vault。

## 最后建议
- 持续更新系统文件，尤其是 `user.md`。
- 先从低风险、长期拖延的任务开始委派。
- 每周迭代一次系统，让代理越来越懂你的业务和工作流。

## 完整中文脚本
# 用 OpenClaw + Obsidian 搭建最适合协作的 AI 代理系统

## 视频信息
- 来源：YouTube
- 链接：https://www.youtube.com/watch?v=5_JN4kfr-9o
- 脚本来源：whisper
- 原始语言：en

## 完整中文整理稿

今天我想向你展示，如何用你的 OpenClaw agent 搭建一套几乎完美的协作系统。

我的这套系统已经跑了大概一周，基本算是 24/7 持续运行。现在外面关于这类东西的信息很多，我相信你应该已经看过不少了。但我想做的是，不是一下子把所有东西都堆上去，而是和我的 OpenClaw 一点一点地搭建，在我原本就已经用 Claude Code 等工具进行的工作流基础上继续扩展。

而它真正能做到的事情，确实非常夸张。

我现在会让它做很多事，比如跟踪我的 YouTube 数据分析、搭建 dashboard、开发完整的应用、和我一起协作内容生产，包括 YouTube、newsletter、Substack，还有各种 side project。它基本都在同时推进。

我的 OpenClaw 是跑在一台隔离的 Mac mini 上，我一方面通过那台 Mac mini 来控制它，另一方面也会通过我自己的个人设备来协作。也就是我现在录这个视频时所用的这台 MacBook Pro。

今天我主要想展示的是，我具体是怎么把这一切搭起来的。我目前觉得最好的方法，就是把 OpenClaw 和 Obsidian 结合起来用。

不过在开始之前先快速说明一下：这个视频不会讲怎么安装 OpenClaw。网上已经有很多这方面的视频了，所以你最好先把那些内容看完。也就是说，如果你要看这个视频，你应该已经对 OpenClaw 的基本工作方式有概念了。如果还没有，那就先去看安装和基础使用的视频，再回来。这个视频的重点完全在于围绕 OpenClaw 搭建的系统，以及如何和它一起协作。

接下来我会讲几件事：

第一，是那些真正让你的 agent 变得有用的系统文件，以及这些文件该怎么设置。

第二，是 Obsidian 为什么会成为你的主要协作空间。

第三，是双机方案，也就是我现在使用的方式：我自己的电脑，加上我 agent 的电脑。

第四，是我这一周里总结出来的一些经验：哪些事情我喜欢委派给 OpenClaw，哪些事情我还是更愿意自己做。

我也想顺便谈谈这个部分。

看一下我这里这份演示文稿。这个演示文稿其实也是我和 OpenClaw 一起做出来的，它帮我完成了这支 YouTube 视频的内容准备。这个主题就是：最理想的 OpenClaw 配置，我如何和 AI agent 进行 24/7 协作。

顺便说明一下，我自己的 agent 叫做 Proxy，这是它给自己取的名字。所以从这里开始，如果我说到 Proxy，说的就是我的 OpenClaw agent。

很多人安装完 OpenClaw 以后，其实就停在那里了。虽然他们也在用它，但并没有真正围绕它建立一套系统。也就是说，他们没有设计出特别顺畅的协作方式。实际使用起来可能会很笨重，也会让人困惑。你会看到到处都是 markdown 文件，到处都是配置文件。

所以这个视频的重点，就是帮助你把这些东西梳理清楚。尤其如果你不是开发者，但又希望把 OpenClaw 用得非常有价值，这套方法会很有帮助。

## 一、系统文件：真正决定代理是否有用的基础

首先，就是系统文件。

如果你已经熟悉 OpenClaw，你应该知道这些就是主要的系统文件。我这里额外加了一个文件，等会会讲到。但先说基础部分：这些是每个人在启动自己的 OpenClaw agent 时都会拥有的核心文件。

第一个是 `user.md`。

我之所以先讲这个，是因为我认为它最重要。我在这个文件上花的时间也是最多的。

这个 `user.md` 文件，写的是关于你和你的业务的一切。我的做法是把所有相关内容都放进去：我是谁、我卖什么、我的客户是谁、我的内容策略是什么、哪些表达该用、哪些表达不要用。

对我来说，这个文件就是我整个业务和品牌的单一事实源。

然后是 `agents.md`，它更像是 agent 的主要系统指令，定义它如何运作、有哪些规则、安全边界等等。

再然后是 `soul.md`，它更偏向于 agent 的人格设定。说实话，这部分我目前还没花太多精力，因为现阶段我没有特别在意这一块。以后可能会再处理。但确实有很多人会在 `soul.md` 上做出很有意思的东西。

接着是 `identity.md`，这里会定义你的 agent 的名字、角色、气质。就像我前面说的，我的 agent 叫 Proxy。

还有 `tools.md`，这里记录你所有本地工具和连接方式。

最后是 `memory.md`，也就是 OpenClaw agent 持续维护的长期记忆。

这些文件在你开启新 session 时，都会被注入到 system prompt 里。所以它们实际上构成了你整个 agent 的底层基础。你真的应该花时间认真打磨这些文件。

而这也是为什么我用 Obsidian 来构建协作系统。因为它会让这些非常重要的文档更容易被编辑、更容易持续迭代，而这些文档又会从根本上改变你使用 OpenClaw 的体验。

如果你看这个截图，等会我们会进入 demo，但这就是它在 Obsidian 里面的样子。

平时你使用 OpenClaw 时，这些通常只是一些文件夹。比如在我的 Mac 上，它们就只是普通文件夹，你会看到一堆 markdown 文件夹和 markdown 文件。

但我想要的是一种更干净、更清晰、可以随时编辑的视图，而这正是 Obsidian 能提供的。

你可以看到，在 Obsidian 里，我可以直接读取这些 MD 文件，可以编辑、输入、修改等等。

这里我还想再多讲一两分钟关于 `user.md`，因为我真的觉得它太重要了。我希望每个人都尽可能在 `user.md` 上投入足够多的精力。

如果你了解我过去一两年一直在搭建的一些系统，你会知道，我过去很强调“context profiles”。

我一直都会讲 context profiles，不管是 JSON 格式的 context profiles，还是 markdown 格式。它们本质上就是名字所说的那样，是关于你自己和你业务各个方面的上下文档案。

以前我的做法是：我会构建自己的 Claude system prompts、Claude.md 文件，然后让它总是去读取这些 context profiles。

而现在，`user.md` 某种程度上就把这一切替代掉了。因为我们现在可以把所有这些上下文，直接塞进 `user.md` 里。

我过去的工作方式是把上下文拆成多个独立文件夹。比如会有一个 business profile，一个 ICP（理想客户画像）profile。然后我还得手动告诉 AI 去查看这些 context profiles，或者把这些要求写进系统指令里。

这样做很容易忘，也很容易失去对整体的掌控。因为你可能有一个理想客户画像文件，又有一个产品和价格文件，还有 newsletter 文件，等等。凡是需要上下文的东西，你都可能单独做一个文件。

而问题在于，文件一多，你往往就维护不过来了。至少对我来说，这就是我以前遇到的情况。

但现在，我把所有东西都放进 `user.md`。

于是我只需要在 workspace 根目录放一个 `user.md` 文件。它每次都会自动加载，里面包含一切：我的业务、我的 ICP，以及所有整合后的背景信息。

这样 agent 每次都能理解我的世界。我也可以在 Obsidian 里随时编辑它，始终盯着它，确保我和 agent 在业务与品牌层面完全对齐。

这就是我的 `user.md` 在 Obsidian 里的样子。你能看到里面会写“关于我”“我的起源故事”“我的专业能力”，还有我以前谈过的其他内容。

## 二、文件夹结构：如果组织混乱，代理再强也没用

在进入 demo 之前，我们先看一下我的文件夹结构长什么样，因为我觉得这个也非常重要。

agent 本身确实很强大，但如果你的 workspace 和文件夹组织方式不清晰，它的价值就会大打折扣。

所以，无论是用 OpenClaw，还是用其他任何 AI，最关键的能力之一就是组织能力。而这种组织能力，最终落实到的就是：你怎么整理自己的文件夹和文件。

这是我的结构。相关内容我都会放在下面。

你可以看到，在我的 workspace 里，也就是 OpenClaw 主要工作的那个总文件夹中，包含了 `agents.md` 以及前面提到的所有 MD 文件。

除此之外，我还额外加了一个 `todo.md`，把它当作共享任务列表。

然后我有一个专门放我自己工作内容的子文件夹，叫 `Alex`。

在这个文件夹内部，我又分了 `dashboards`、`Substack`、`YouTube`、`email sequences`、我正在做的 side projects、`assets`、配置文件（比如我的 MCP）、memory，以及所有 agent skills。

而且这些内容我都可以直接在 Obsidian 里打开。

如果你面对的是不同客户，你可能会按照客户维度来组织。如果你的 OpenClaw 同时用于个人和工作，你也可以按这个逻辑去组织。

但重点就是：一定要持续地、尽可能认真地整理你的文件结构。

## 三、双机方案：一台给自己，一台给代理

我最后做的，就是“两个机器，一个 workspace”的方案。

左边你看到的是我的机器，也就是我自己每天使用的 MacBook Pro。

我喜欢把那些我想亲自上手的工作，继续保留在自己手上。我还是会用 Claude Code、Claude Cowork 来构思内容、想自己的点子、做创意类工作，也会在本机临时启动一些 agent 与我并肩工作。

而右边这台机器，也就是给 agent 的第二台机器，是一台用于运行 OpenClaw 的 Mac mini，它 24/7 持续在线，是 agent 的主基地。

最开始我希望它做的事情，是处理那些溢出的工作，也就是那些我一直没有时间做的事情。

所以从根本上说，这台 agent 机器现在负责的就是这类工作。

而我和它之间的连接，是通过 Obsidian 完成的。我们在这个共享协作空间中一起工作。

当然，这种分工会随着时间慢慢变化。比如今天，我就让 Proxy 参与了这份演示稿，也让它参与了这支 YouTube 视频的准备。

所以具体分工会变化。

但我相当确定的是，这种双机模式本身不会变。

我的 agent 将一直负责那些我本来没有时间处理的事情，处理所有额外的工作。

你可以把它想象成一个员工。

如果你真的招了一个员工，你不会在第一天就把所有事情一股脑丢给他。你会先把溢出任务交给他，让他慢慢熟悉你的工作流，然后再一点一点给他更多责任。

我现在就是用同样的方式来对待我的 OpenClaw AI agent。

## 四、实际演示：我如何在个人设备、Obsidian 和代理机器之间切换

现在进入实际 demo。

你现在看到的是我的个人电脑，也就是我的 MacBook。左边是 Obsidian，右边是我通过 iMessage 接到 Proxy 的聊天窗口。

然后这里还有一个屏幕共享窗口，我需要登录一下。这个窗口连接的就是我的 Mac mini。我是通过屏幕共享进入那台机器的。

这就是我的工作方式。

我在自己的 MacBook 上能看到一切，也可以很快切换到 Proxy 的 workspace。

这套方式对我来说运行得非常顺手。

我不打算深入讨论 Mac mini 和 VPS 哪个更好。我更喜欢 Mac mini，原因有很多，其中最重要的一点是：我喜欢把完整的环境直接放在眼前。

我喜欢看到桌面，喜欢在任何时候都可以直接屏幕共享进入 Mac mini，看看 Proxy 在做什么、看看我的 agent 在做什么。

我也喜欢 iMessage。总之，这一整套东西对我来说就是更顺手。

这并不是说你不能用 VPS，只是我自己不是这么做的。

现在来看 Mac mini 本身。这就是 agent 的 workspace。

当你进入 OpenClaw agent 时，在左下角你会看到那个文件夹。这个文件夹就是 workspace folder，你现在应该已经很熟悉了。

这个 folder 里就是前面提到的一切：系统 MD 文件、配置、agent skills，以及你放进去的其他任何文件夹。

可以说，这是你和 OpenClaw agent 协作时最重要的文件夹。

在它上方你能看到聊天区。

而我做的事情是：在右边安装了 Obsidian。我下载了 Obsidian，并且订阅了 Obsidian Sync，大概每个月 5 到 10 美元。

这个服务允许你打开一个 Obsidian Vault，并在不同设备之间共享。

所以我现在的做法是：直接在这个 workspace folder 里创建了一个同步的 Obsidian Vault。也就是说，我就是把这个 workspace folder 当成 Obsidian Vault 使用。

这样一来，你就能在 Obsidian 里看到一切。

我们可以查看所有不同的文件，可以查看里面的所有内容，包括我们的内容资料。比如我这里有 YouTube 相关内容和 transcripts，我也可以看到 user files。

而这会带来巨大的变化。

因为你不再只是像以前那样，在左下角看着一堆原始 markdown 文件。现在我们有了一个真正优秀的协作空间，我们可以在里面看到一切、不断编辑它们、持续打磨这些系统文件，让它们变得越来越好。

一旦你下载了 Obsidian 并配置好 Sync，整个过程其实非常简单。外面有很多文档可以跟着做。

你只需要下载安装 Obsidian，查一下 Sync，然后在这个 workspace folder 里把它打开。

做完之后，你就可以共享它了。

## 五、把代理当成独立成员：专属账号、专属空间、专属权限

我现在的配置是这样的：Proxy，也就是我的 agent，有一个完全隔离的 workspace。

我几乎就是把它当作我公司的一个员工来对待。

Proxy 有自己的 Google Workspace，有自己的公司邮箱，有自己的电话号码，有自己的 iMessage，甚至还有一张额度受限的公司信用卡，用来支付订阅之类的费用。

现在它甚至也有了自己的 Obsidian 账号。基本上，它有自己的一切。

所以，这个 Obsidian Vault 本质上是挂在 Proxy 名下的。

然后我再把这个 Vault 分享给我自己的邮箱。

接下来我切回我的个人机器，也就是我的 MacBook。我只需要接受这个共享 Vault，现在它就出现在我的设备上了。

于是我们两个就在同一组文件上工作。

这样一来，在我的个人设备上，我也能看到一切。我可以看到 agent 文件、user 文件，Proxy 能看到的东西我都能看到。

同时，我还能在 iMessage 里一边和它对话，一边协作。

## 六、让 Obsidian 真正好用的关键：显示非 Markdown 文件

当你把 Obsidian Vault 搭好，并且让它在“你的机器”和“agent 的机器”之间同步之后，还有几件非常重要、而且很酷的事情你需要在 Obsidian 里做，这会让整个体验提升很多。

第一件事，就是让非 Markdown 文件也可见。

因为默认情况下，Obsidian 主要只会显示 Markdown 文件。也就是说，像 `TXT`、`HTML`、`JSON` 这些文件，默认是不会出现在 Obsidian Vault 视图里的。

但你和 agent 协作时，实际上会经常处理这些文件，比如 HTML dashboard、应用文件、文本转录稿、JSON 配置等等。

你肯定也希望能在自己的个人设备上看到这些东西。

所以，我们要借助 Obsidian 的两个插件。

这些都是开源的 community plugins，可以直接安装到 Obsidian 里。

做法很简单：打开 Obsidian，进入设置。

在设置里找到 Community Plugins。如果你是第一次操作，先启用社区插件。

然后进入插件浏览页面。

我会把需要的两个插件放在下方，它们分别是：
- Data Files Editor
- Obsidian HTML Plugin

这两个插件的作用分别是：

第一个插件可以让你查看和编辑 `JSON`、`TXT` 以及其他非 Markdown 文件。也就是说，你可以直接看到像 MCP 配置文件、package 文件之类的内容。

第二个插件可以让你在 Vault 里直接渲染 HTML 文件。

你把它们安装并启用之后，就像我现在这样：我这里已经启用了 Data Files Editor 和 HTML Reader。

一旦这样设置完成，这台机器上就能正常使用这些能力了。

然后你要在 OpenClaw agent 那台机器上的 Obsidian 里也做同样的操作。

也就是说，两台机器都要安装同样的插件。

如果你不想手动操作，也可以让 OpenClaw 帮你，它可以自己完成这件事。

## 七、插件启用后，你的 TXT、HTML、JSON 都能进入协作空间

一旦你做完这些配置，你就可以实现很多很酷的事情。

比如你看我这里，在 `Alex` 文件夹下面有 `YouTube`，然后里面有 `transcripts`。这些都是 `TXT` 文件，你可以看到文件名旁边就标着 TXT。

现在我已经可以直接在 Obsidian Vault 里读取这些 TXT 文件了。

但更酷的还不止这个。

真正很厉害的是，你还能直接渲染 HTML。

比如我最近几天让 Proxy 帮我做了一个 YouTube analytics dashboard。

现在这个 dashboard 已经做好了，Proxy 把它放进了我们的 `dashboards` 文件夹，然后我就可以在我自己的个人设备上，直接通过 Obsidian 把它渲染出来。

这真的非常酷。

因为我最近就在想：我其实已经不再需要 Airtable 了，也不需要 Notion 了，甚至也不需要专门的数据库工具来跟踪我的数据分析。

我完全可以让 OpenClaw 去做分析、给我做 dashboard，然后持续为我更新。

我现在的方式是：通过一个 YouTube MCP，把它连接到我的 YouTube API，再把所有统计数据和分析拉进来，然后生成这个自定义 dashboard 给我看。

这已经是非常高阶的玩法了，真的很强。

我几乎找不到词来形容这件事到底有多夸张。

而现在，有了这些插件以后，不只是 HTML，TXT 文件、其他文件也都能这样用起来。

## 八、另一项关键设置：同步所有不受支持的文件类型

还有第二件我们必须做的事情，才能让 Obsidian 和 OpenClaw 的配合真正顺畅。

那就是：开启对“不受支持文件类型”的同步。

回到 Obsidian 设置里。记住，你在一个 Vault 里做的配置，通常都要在两台机器上都做一遍，也就是你自己的 Obsidian 和 agent 的 Obsidian。

在设置中找到 Sync。

往下滚动，你会看到一个选项，大意是“同步所有其他文件类型”。

你必须在两台机器上都把这个开关打开。

因为如果你不打开，`HTML`、`JSON`、`TXT` 这些文件永远不会在两台机器之间同步。

默认只会同步图片、音频、视频、PDF 之类的文件，但我们真正需要同步的是所有这些工作文件，比如 HTML 和 JSON。

所以你要在两台机器上都把它打开。

一旦做完这一步，你基本就拥有了一套完整、顺畅的 OpenClaw + Obsidian 协作系统。

最后，在 Obsidian 底部你会看到一个小对勾图标。这表示当前两个 Vault 之间已经完全同步，也就是你机器上的 Vault 和 agent 机器上的 Vault 已经保持一致。

如果你看到的是一个紫色旋转的圆圈，那就表示它还在同步中。

所以，当你打算和 OpenClaw agent 讨论某个 Vault 里的东西时，先确认它已经同步完成。如果没有，就稍微等一两分钟。

## 九、实际协作方式：我怎么让 Proxy 直接处理内容任务

一旦这些都配置好了，你会解锁很多工作方式。

比如，你的 MCP servers 现在也可以在两台机器上同时看到。像我这里这个 YouTube MCP，我两边都能看见。

换句话说，你放进去的任何内容，基本都能共享可见。

为了让你更直观理解这套协作是怎么运作的，我给你看一下我的 Proxy 聊天窗口。

假设我现在在 Substack 或者 YouTube 相关目录里工作。

我这里有 YouTube transcript，也有 Substack 文件夹，还有所有 agent skills。现在这些 skills 我也都能直接在 Obsidian 里看到。

比如，如果我想用一个“thought leadership newsletter”的 skill，我也能在这里直接看到。

这种感觉真的很好，因为一切都在手边。

我可以直接对 Proxy 说：

“Proxy，把我最新的 YouTube transcript 转成一篇 thought leadership newsletter。完成之后，把所有内容都放到 Obsidian Vault 里正确的位置。”

然后 Proxy 就会真的去做。

它会先从这里读取 transcript，再调用相应的 skill，生成内容，然后把结果重新放回 Obsidian Vault 对应的位置。

而我会直接看到那个文件落在正确的目录里。

整个过程中，我几乎都不需要真正进入 agent 的机器，也不用反复处理各种琐碎操作。因为一切都在这里。

这就是我现在每天工作的方式。

我通常在自己的 MacBook 上工作，打开 iMessage 和 Proxy 对话，同时打开 Obsidian 来查看我们共享的 workspace。

而只要我想更直接地观察它在做什么，或者想和它更紧密地一起工作，我就切到屏幕共享窗口，直接跳进 Mac mini。

这样我就进入了 Proxy 的主机，可以直接在那边的聊天窗口里和它一起做更细的操作。

这套系统真的是我目前认为最理想的协作方式。过去这一两周，我靠它完成了非常多的事情，多到有点夸张。

## 十、这周我具体把什么交给了 Proxy，什么还留给自己做

最后我想用几点收尾，讲讲我过去这一周左右，到底把哪些事情交给了 Proxy，也就是我的 OpenClaw agent，哪些事情我还是留给自己来做。

当然，这些分工未来一定还会继续变化。

就我个人而言，我现在仍然会亲自做的，是那些需要我自己动手、而且我也享受其中的工作。

比如我会继续亲自使用 Claude、Claude Code 和 Claude Cowork 来处理我的 YouTube 视频，来构思选题，把它们转成 newsletter，完成核心内容创作。

其实本质上就是：凡是我真正喜欢做的事情，我都还是在自己的机器上亲自做。

而 OpenClaw agent 则负责那些像 analytics tracking、dashboards 这样的工作。这些事情以前我根本没有时间做。

我算是那种非常典型的单人创业者。我从来没有花钱请外部帮助，也从来没有真正雇过人。我的所有业务基本都是我自己一个人运营起来的。

也正因为如此，说实话，很多事情确实一直被耽搁了。

比如我根本没办法把所有本该做的数据分析和跟踪都做完。

但现在，正如你前面看到的，Proxy 正在帮我追踪这些 analytics，还会给我做 dashboard。这真的帮了大忙。

还有社交媒体。

我过去从来没有足够时间花很多精力在社交媒体上。我也没时间长期投入在 Twitter、X 或 LinkedIn 上。

Proxy 现在也在帮我处理这些事情。

除此之外，它还会帮我构建完整的应用和内部工具。正如你前面看到的，它在帮我搭建我的整个 co-writer system，也就是我的专属社区和会员平台。

它也在帮我做 dashboard，也在推进我的 side projects，也在帮我整理 workspace。

可以说，它正在帮我完成所有那些我一直想做、但从来没时间做，也从来没花钱外包去做的事。

所以，就像我前面说的：把 OpenClaw 当成员工，而不是替代品。

你不会在新员工上班第一天，就把你最重要的工作交出去。你会先把那些溢出的任务给他。

就在这一周，我们已经一起做了很多东西。比如连接 YouTube Data API，构建自定义 dashboard，等等。前面这些内容我们已经讲过了。

## 十一、作者最后的建议

总结来说，我会这样建议你：

第一，当你把 OpenClaw 跑起来之后，优先去打磨 `user.md`。

在这个文件上投入大量时间，而且永远不要忽视它。

你甚至可以每周或每两周审查一次，持续往里补充内容，比如你的业务、产品、ICP、内容策略等等。

这是投入产出比最高的一件事，因为这样一来，你和你的 OpenClaw 就能始终站在同一页上，像一对真正对齐的协作伙伴一样去完成目标。

第二，配置 Obsidian。

把它安装在两台机器上，开启 Sync，安装需要的插件，启用所有文件类型的显示和同步。

第三，开始整理你的 workspace。

把文件夹结构、工作资料、资产文件都尽可能整理干净，并且持续维护。

第四，从小处开始。

不要一开始就把你最重要的工作全交给它。先从那些你一直拖着没做、或者根本没时间做的任务开始。最好是那种即使让它自己跑，也不会严重影响你现有核心工作流的事情。

不要现在就把所有东西一把扔掉，然后让 OpenClaw 去接管一切。

第五，持续迭代。

不断更新你的系统文件，随着你越来越清楚什么有效、什么无效，系统也会每周变得更好。

它会一边运行一边学习，而这个过程真的非常惊人。

所以，试试看吧。

我会把相关资源都放在下方。如果你也在做类似的事情，欢迎留言。去拿这些资源吧。未来我也还会继续分享更多关于 OpenClaw 的内容。

## 归档备注
- 这支视频的核心价值不在于安装教程，而在于“如何把 AI agent 纳入一个可持续的个人工作系统”。
- 作者最强调的是三个支点：`user.md`、Obsidian 共享工作区、双机协作。
- 作者的心智模型很明确：先把 AI 当作处理溢出任务的新员工，再逐步扩大其责任范围。

<!-- capture_id:a86bb509-08f4-4fc7-a984-f3a86362d702 -->